/** PostCSS config for tooling that reads this file (e.g. IDE plugins). Vite uses `vite.config.ts` `css.postcss`. */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
